name: todo_test_task
description: A Flutter test task for candidates to complete missing functionality.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  http: ^1.5.0
  cupertino_icons: ^1.0.8
  flutter_speed_dial: ^7.0.0
  get_storage: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
